name: Finalize UC02 DEV acceptance evidence

on:
  pull_request:
    branches: [dev]

permissions:
  contents: read

jobs:
  finalize:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Final evidence paused while resolving Railway Postgres public/TCP proxy metadata.'
