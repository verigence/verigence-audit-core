name: Verify UC02 DEV end-to-end

on:
  pull_request:
    branches: [dev]

permissions:
  contents: read

jobs:
  verify:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'E2E creation paused while recovering the existing tagged UC02 fixture.'
