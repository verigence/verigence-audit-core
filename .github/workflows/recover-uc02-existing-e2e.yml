name: Recover existing UC02 DEV acceptance fixture

on:
  pull_request:
    branches: [dev]

permissions:
  contents: read

jobs:
  recover:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Distributed provisioning recovery already completed; continuation runs separately.'
