# UC03 Customer Mobile PII — Implementation Status

- Design approved: 2026-08-27
- Branch: `feature/customer-mobile-pii`
- Goal: persist full mobile in Audit Core, mask in normal Customer API responses, reveal only with `audit.customer.contact.full.read`.
- No new API endpoint.

Implementation work in progress.
